# FlowState binary path
#
# Set the absolute path to a built FlowState backend binary so the
# dev-server script can find it. Overrides PATH lookup.
#
#   FLOWSTATE_BIN=/home/user/flowstate/build/flowstate
#
FLOWSTATE_BIN=/home/baphled/FlowState.git/agent-platform/build/flowstate
